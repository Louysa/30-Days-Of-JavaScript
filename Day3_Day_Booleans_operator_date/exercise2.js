/* const base = prompt('Enter the base of the triangle:');
const height = prompt('Enter the height of the triangle:');
alert(`The are of the triangle is : ${ base * height / 2} `); */
const test = new Date();
const year = test.getFullYear();
const month = test.getMonth() + 1;
const date = test.getDate();
const hour = test.getHours();
const minute = test.getMinutes();
console.log(year,'-',month,'-',date,'-',hour,':',minute);